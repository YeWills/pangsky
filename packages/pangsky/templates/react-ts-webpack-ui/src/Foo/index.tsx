import React, { type FC } from 'react';
import './style.scss';

const Foo: FC<{ title: string }> = (props) => (
  <div>
    <div className={'pppp'}>9919887</div>
    <h4>{props.title}</h4>
  </div>
);

export default Foo;
